#include <iostream>

using namespace std;

int main(){
    double x, y;
    double S=0;
    cout.precision(2);
    cout.setf(ios::fixed);
    cin >> x >> y;
    S = x/y;
    cout << S << endl;
    
}
