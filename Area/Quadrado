#include <iostream>
using namespace std;

int main () {
int L;                        // Sendo L o lado do quadrado
    cin >> L;                 // Defina L 
    cout << L*L << endl;      // Expresse L multiplicado por L, basicamente, L ao quadrado
}
